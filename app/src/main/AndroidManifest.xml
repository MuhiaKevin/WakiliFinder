<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wakilifinder.wakilifinder">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- android:theme="@style/Theme.AppCompat.NoActionBar"> -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ClientLogin" />
        <activity android:name=".ClientSignup" />
        <activity android:name=".HomeClient" />
        <activity android:name=".LawyerLogin" />


        <activity
            android:name=".LawyerSignup"
            android:label="@string/title_activity_lawyer_signup2_maps" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".HomeLawyer"/>
        <activity android:name=".ClientPay"/>
        <service
            android:name=".FetchAddressIntentService"
            android:exported="false"/>
    </application>

</manifest>